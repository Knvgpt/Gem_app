import React from "react";
import { useEffect } from "react";
import axios from "axios";

export default function About() {
  useEffect(() => {});
  // todo throw the axious code for gettig the data from the express api we created.

  return <h1>About</h1>;
}
